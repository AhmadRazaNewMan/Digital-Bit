# Copy to ".env" in this folder (same level as scripts/). Never commit .env.
GROQ_API_KEY=

# Optional — defaults work for the daily generator (OpenAI-compatible chat).
# LLM_MODEL=llama-3.3-70b-versatile
